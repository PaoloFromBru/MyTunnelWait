// app/viewport.ts
import type { Viewport } from "next";

const viewport: Viewport = {
  themeColor: "#111111",
  width: "device-width",
  initialScale: 1,
  colorScheme: "light",
};

export default viewport;

